---
# Packages are defined in vars/family/RedHat.yml
- name: Make sure packages are installed (RedHat family)
  become: true
  ansible.builtin.dnf:
    name: "{{ packages }}"
    use_backend: dnf4
  when:
    - packages is defined
